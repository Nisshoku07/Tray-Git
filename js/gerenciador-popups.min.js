window.addEventListener('load', function() { const e = document.getElementById('popup-entrada'),
        t = document.getElementById('popup-saida'),
        o = (new Date).toISOString().slice(0, 10); let a = !1; if (e) { const t = 'popup_entrada_exibido_v1',
            n = localStorage.getItem(t);
        n !== o && (e.style.display = 'flex', a = !0, localStorage.setItem(t, o)); const i = e.querySelector('.close-btn');
        i && i.addEventListener('click', function() { e.style.display = 'none', a = !1 }) } if (t) { const e = 'popup_saida_exibido_v1',
            n = () => { if (a) return; const n = localStorage.getItem(e);
                n !== o && (t.style.display = 'flex', a = !0, localStorage.setItem(e, o)) };
        document.addEventListener('mouseout', function(e) { e.clientY <= 0 && n() }); let i = window.scrollY;
        document.addEventListener('scroll', () => { window.scrollY < i && window.scrollY < 100 && n(), i = window.scrollY }); const s = t.querySelector('.close-btn');
        s && s.addEventListener('click', () => { t.style.display = 'none', a = !1 }); const l = t.querySelector('a');
        l && l.addEventListener('click', () => { t.style.display = 'none', a = !1 }) } });